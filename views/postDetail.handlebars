{{#extend "main"}}
  {{#content "body"}}
    <article>
      <h1>{{post.title}}</h1>
      <p>{{post.content}}</p>
      <p>Posted by: {{post.author}}</p>
      <p>Date: {{post.date}}</p>
    </article>
    <!-- Add comment section here if needed -->
  {{/content}}
{{/extend}}
